{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.name) }}</div>
    <div class="col-md-6">{{ form_row(form.schoolar_level) }}</div>
</div>

<div class="row">

    <div class="col-md-6">{{ form_row(form.school) }}</div>

    <div class="col-md-6">{{ form_row(form.birthday_date) }}</div>
</div>




<button class="btn btn-primary"> {{ button|default('child.create')|trans }} </button>
{{ form_end(form) }}